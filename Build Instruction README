Usage: ./SWE_gnu_release_none_fwave -x GRID_SIZE_X -y GRID_SIZE_Y [-t SIMULATION_TIME] [-b BOUNDARY_CONDITION] -o OUTPUT_BASEPATH [-h]

non-optional arguments:
  -x, --grid-size-x GRID_SIZE_X
                              Number of cells in x direction
  -y, --grid-size-y GRID_SIZE_Y
                              Number of cells in y direction
  -o, --output-basepath OUTPUT_BASEPATH
                              Output base file name
  -h, --help                  Show this help message

optional arguments:
  -t, --simulation-time SIMULATION_TIME
                              Number of seconds of simulation
  -b, --boundary-condition BOUNDARY_CONDITION
                              1: OUTFLOW ,2:WALL ,3:INFLOW, 4:CONNECT ,5:PASSIVE

To switch scenario, edit the integer "switchScenario" in src/examples/swe_simple.cpp line 156.

To compile the code use the commands:
scons
cd build/
./SWE_gnu_release_none_fwave -x 100 -y 100 -t 1000 -b 1 -o "../build/"

the output file is named "_00.nc"


